
const nextConfig = {
    output: 'export',
    basePath: '/bible-riddles-quiz',
    typescript: {
        ignoreBuildErrors: true,
    },
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
